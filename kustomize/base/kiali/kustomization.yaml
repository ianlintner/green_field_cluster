apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - configmap.yaml
  - service.yaml
  - deployment.yaml

secretGenerator:
  - name: kiali
    literals:
      # ⚠️ SECURITY WARNING: Default password for demonstration only
      # DO NOT use in production! Change before deploying to non-dev environments
      - passphrase=changeme123
